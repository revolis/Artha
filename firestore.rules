rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All app access should go through authenticated API routes via Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
